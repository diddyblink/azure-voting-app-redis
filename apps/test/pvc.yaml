apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-1
  namespace: voting-app-test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: target


